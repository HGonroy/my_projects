#include <stdio.h>
#include <stdlib.h>

int main ()
{
    int x, y = 10, z = 12;
    float a, b = 5.25, c = 10.5;
    x = y + z;
    a = b + c;
    printf ("x = %d\n", x);
    printf ("a = %f\n", a);
    system ("pause");
    return (0);
}